apiVersion: v1
kind: Service
metadata:
  name: {{ include "upgrade_rlbk.fullname" . }}
  labels:
    {{- include "upgrade_rlbk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "upgrade_rlbk.selectorLabels" . | nindent 4 }}
