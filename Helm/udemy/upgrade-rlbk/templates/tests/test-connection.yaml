apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "upgrade_rlbk.fullname" . }}-test-connection"
  labels:
    {{- include "upgrade_rlbk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "upgrade_rlbk.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
